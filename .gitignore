# macOS
.DS_Store
._*

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code (install target is .claude/, source is framework/)
.claude/

# SDD project data (install target — tracked in dev repo)
# .sdd/ is NOT ignored: this is the framework dev repo, session/project data is part of the source

# Local-only directories
docs/
examples/
