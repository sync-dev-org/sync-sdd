# sdd-review-self-ext 改善セッション (2026-03-04)

## 実施済み

1. **tmux セッション分離**: SID (pane ID ベース)、命名規則 `sdd-{SID}-{purpose}-{identifier}`、tiled レイアウト、Orphan Cleanup 確認付き
2. **CLAUDE.md**: `$()` コマンド置換回避ルール追加
3. **settings.json**: `bash install.sh *`, `date *` 許可追加
4. **shared+per-agent プロンプト分離**: 5 並列 Write、cat 結合で stdin に渡す
5. **B4 ext review (Codex)**: 4/4 完了。H5 M4 L2 = 11 findings, FP 3。自明修正 5 件実施
6. **Step 8.2 レポート改善**: auto-fix vs decision-required の 2 カテゴリ提示
7. **Builder `execution_plan` → `execution`**: 実バグ修正 (B4 H5)

## 未着手 (Skill 起動後の全フィードバック)

### T1: Pane 一斉解放パターン
Codex は完了時に pane が即座に閉じる。全 Agent 揃うまで pane を維持したい。
```
現状: {cmd}; tmux wait-for -S sdd-{SID}-ext-{N}
改善: {cmd}; tmux wait-for -S sdd-{SID}-ext-{N}; tmux wait-for sdd-{SID}-close
```
Lead が全 Agent 完了 → CPF 収集 → `tmux wait-for -S sdd-{SID}-close` で一斉解放。
影響: sdd-review-self-ext/SKILL.md (Dispatch Mode), tmux-integration.md (One-Shot Command pattern)

### T2: tmux split の最適化
tiled 導入で `-t $PREV` チェイン不要。全 split を Lead pane から実行し簡素化。
pane ID は Step 4 の before/after 比較で取得可能。
影響: sdd-review-self-ext/SKILL.md (Dispatch Mode)

### T3: shared+per-agent プロンプト分離の動作検証
SKILL.md に記述済みだが実際のレビュー実行で未検証。確認項目:
1. 5 並列 Write が期待通り並列実行されるか
2. `cat shared-prompt.txt agent-N-prompt.txt | {engine_cmd}` で正しくプロンプトが結合されるか
3. 各エンジン (codex/claude/gemini) で stdin 結合が動作するか
影響: sdd-review-self-ext/SKILL.md (Step 2, Step 5)

### T4: レポートの判断上申パターン検証 + SKILL.md 調整
Step 8.2 に auto-fix vs decision-required の 2 カテゴリを定義したが、実際の動作検証が未了。
確認項目:
1. 自明修正が正しく分類され「OK で全件修正します」と提示されるか
2. 判断要求項目に影響範囲・推奨・理由が添えられるか
3. SKILL.md の記述で Lead が期待通りの振る舞いをするか（記述の曖昧さ・不足がないか）
影響: sdd-review-self-ext/SKILL.md (Step 8.2)

### T5: B4 deferred findings
- H2-H4: Cross-cutting/wave QG backlog (review.md, revise.md, run.md)
- M2-M3: publish-setup backlog (sdd-publish-setup/SKILL.md)
- L1: consensus Usage 不足 (review.md)
