# Session Handover
**Generated**: 2026-03-05T00:54:41+0900
**Branch**: main

## Direction

### Immediate Next Action
`--engine subagents` でテンプレート移行後の動作検証。Prep → Inspector → Auditor が承認プロンプトなしで完走することを確認。

### Active Goals
- **Review パイプライン tmux 化** — Phase 1 (review-self SubAgent エンジン + テンプレート移行) 完了、Phase 2 (sdd-review 統一スキル) は次セッション以降
  - Phase 2 計画: sdd-review スキル新設、22 SubAgent → プロンプトテンプレート移行 (4-6 セッション見積)
  - プラン詳細: `.claude/plans/soft-baking-donut.md`
- **SPEC-UPDATE-NEEDED ループに Design Review 追加** (D116 defer)
- **Pre-existing backlog** — cross-cutting verdict path, Blocking Protocol 等
- **`/sdd-steering engines` 対話フロー検証** (carry forward)
- **v1.10.0+ 統合検証** (carry forward): speechflow での sdd-inspector-e2e dispatch
- 残りの改善アイデア: A2 (Run Fingerprint), A3 (Smart Retry), A5 (Adaptive Review Depth)
- **--update 時の settings.json マイグレーション未対応**

### Key Decisions
**Continuing from previous sessions:**
- 開発方針: 本リポはsync-sddフレームワーク自体の開発リポ。spec/steering/roadmapは不使用 (D2)
- SubAgent dispatch はデフォルト background: `run_in_background: true` (D10)
- Cross-Cutting は revise 拡張: feature 名有無で自動判定 (D17)
- E2E を inspector-test から分離し専用 Inspector 新設 (D88)
- MultiView Layout 最終仕様: 1-Lead 4象限12スロット (D115)
- Agent Pipeline 導入 + Lead 監修プロトコル (D120, D121)
- review-self デフォルトエンジン構成: Prep=codex-spark, Inspectors=gpt-5.3-codex, Auditor=claude-sonnet-4-6 (D128)
- Review パイプライン tmux 化方針: sdd-review-self で SubAgent エンジン先行実証 → sdd-review 統一スキルにコピー拡張 (D133)

**Added this session:**
- テンプレート `.sdd/` 移行: `.claude/skills/*/refs/` → `.sdd/settings/templates/` に順次移行 (`.claude/` 保護で cp 時に承認プロンプトが出る問題の解決)
- ワンライナー dispatch: cp + sed を廃止し、テンプレート直読み + sed パイプでエンジン投入をワンコマンド化
- `${}` パラメータ置換禁止: CLAUDE.md に `$()` と並んで `${}` も承認プロンプトの原因として明記
- subagents エンジン検証完了: Prep (haiku) + Inspector ×4 (sonnet) + Auditor (sonnet) が全て CPF 生成成功。フォールバック不要

### Warnings
- **Claude Code が pane タイトルを上書きする**: Step 4 の Lead タイトルからの SID 取得が機能しない。slot タイトルからのフォールバック検出で回避中だが、SKILL.md の記述と実動作に乖離あり
- **tmux send-keys スタック問題**: pgrep ゾンビ確認 + sleep 1 throttle で軽減済みだが根本解決ではない
- **--update 時の settings.json マイグレーション未対応**
- **テンプレート移行後の実行検証未済**: SKILL.md + prep.md を更新したが、移行後のフローでの `/sdd-review-self --engine subagents` は未テスト

## Session Context

### Tone and Nuance
- 判断上申時は材料+推奨+選択肢を添えること。「どうしますか？」だけで聞かない
- session.md は `/sdd-handover` の専任。直接書き込まない
- framework/ 配下を直接実行せず、install 後の .sdd/ パスから実行する
- sed は SKILL 内部のテンプレート置換用途に限定して使用可
- review findings の提示は SKILL.md の提示テンプレートに厳密に従う。要約テーブルへの圧縮禁止
- Session Resume は Step 1-7 を順番に実行する
- **ユーザーの質問には簡潔・正確に答える。的外れな回答が続くと信頼を失う**

### Steering Exceptions
なし

## Accomplished

### Work Summary (this session)
1. **Bash `${}` 禁止ルール追加**: CLAUDE.md に `${}` パラメータ置換の禁止を追記。bash-security-heuristics.md にも記録
2. **subagents エンジン動作検証 (B35)**: `--engine subagents` でフルパイプライン完走。Prep (haiku 78s) + Inspector ×4 (sonnet 125-417s) + Auditor (sonnet 202s)。CPF 4/4 生成成功。結果は検証目的で破棄
3. **テンプレート `.sdd/` 移行**: 6 テンプレートを `framework/claude/skills/sdd-review-self/refs/` → `framework/claude/sdd/settings/templates/review-self/` に移動。`{{SCOPE_DIR}}` / `{{SDD_DIR}}` / `{{TPL_DIR}}` をハードコード化
4. **ワンライナー dispatch 最適化**: active/ へのテンプレートコピーを廃止。テンプレート直読み + sed パイプでエンジン投入をワンコマンド化。SubAgent mode は Bash ゼロ

### Previous Sessions (carry forward)
- v2.1.0: sdd-review-self-ext → sdd-review-self 昇格 + placeholder 統一 + grid.sh 冪等性
- v2.1.0: エンジン精度比較 + B7-B10 backlog fix + consensus 削除
- v2.0.0: MultiView Layout スロットモデル + agent pipeline + auditor + テンプレート化

## Resume Instructions
1. `session.md` を読んで方向性を把握
2. origin/main から 3 commits ahead — push するか確認
3. `/sdd-review-self --engine subagents` でテンプレート移行後の動作検証
4. 検証通過後、Phase 2 (sdd-review 統一スキル作成) に着手 — プラン: `.claude/plans/soft-baking-donut.md`
