# Session Handover

## Direction

### Immediate Next Action
v1.2.0 リリース完了。実プロジェクトで `install.sh --update` を実行し、`.claude/sdd/` → `.sdd/` マイグレーションの動作を検証する。

### Active Goals
- **実運用検証**: .sdd/ パス移行、E2E/Visual 分離、Cross-Cutting revision、acceptEdits モードを実プロジェクトで検証
- 残りの改善アイデア: A1 (Context Budget), A2 (Run Fingerprint), A3 (Smart Retry), A5 (Adaptive Review Depth) — 実運用検証の後に着手

### Key Decisions
- **開発方針**: 本リポはsync-sddフレームワーク自体の開発リポ。spec/steering/roadmapは不使用 (D2)
- **フェーズモデル維持**: 4フェーズのまま (D5)
- **Lookahead追跡は動的再計算** (D6)
- **sdd-review-selfはCommands(6)に含めない**: framework-internal用途 (D2)
- **SubAgent dispatch はデフォルト background**: `run_in_background: true` + agent frontmatter `background: true` (D10)
- **Worktree分離は不採用**: 現状で問題なし (D14)
- **Cross-Cutting は revise 拡張**: feature 名有無で自動判定 (D17)
- **.sdd/ はユーザー成果物**: uninstall でフレームワークファイル (settings/, .version) のみ削除。project/, handover/ は保持。空の .sdd/ ディレクトリも残す（ユーザー成果物の安全を優先）

### Warnings
- install先の `.claude/CLAUDE.md` と `.sdd/` は旧バージョンのまま。実プロジェクトで `install.sh --update` 実行が必要
- 競合リサーチのプランファイル (`~/.claude/plans/peaceful-noodling-comet.md`) に未実装アイデア 15件分の詳細あり

## Session Context

### Tone and Nuance
なし

### Steering Exceptions
なし

## Accomplished

### Work Summary (this session)
1. **acceptEdits 問題調査**: `.claude/` 配下のファイルは Claude Code が「自身の設定ファイル」として特別な権限保護を適用。`acceptEdits` モードでも Edit 確認プロンプトが表示される問題を特定
2. **SDD data root 移行** (.claude/sdd/ → .sdd/):
   - `framework/claude/CLAUDE.md`: `{{SDD_DIR}}` = `.claude/sdd` → `.sdd` (1行変更で全参照が自動解決)
   - `framework/claude/sdd/settings/rules/steering-principles.md`: ハードコードパス2箇所更新
   - `install.sh`: インストール先パス、v1.2.0 マイグレーションブロック、.gitignore管理、アンインストール対応
   - `README.md`: ディレクトリツリー表示更新
   - `.gitignore`: `.sdd/` 追加
3. **Self-review B5 + B6**: B5は1/5エージェントのみ完了（4/5が使用量上限）、B6は5/5全完了。合計21件中19件FP除去、残りL:2のみ
4. **L1修正**: design-principles.md `Components & Interfaces` → `Components and Interfaces` 表記統一
5. **.gitignore重複チェック改善**: `grep -q '\.sdd'` に簡素化（既存エントリの検出漏れ防止）
6. **v1.2.0 リリース**: branch + tag + push 完了

### Modified Files
- `framework/claude/CLAUDE.md` — `{{SDD_DIR}}` = `.sdd`
- `framework/claude/sdd/settings/rules/steering-principles.md` — パス更新
- `framework/claude/sdd/settings/rules/design-principles.md` — 表記統一
- `install.sh` — パス移行 + マイグレーション + .gitignore改善
- `README.md` — v1.2.0 + ディレクトリツリー
- `.gitignore` — `.sdd/` 追加
- `VERSION` — 1.2.0

### Previous Sessions (carry forward)
- v1.1.2: acceptEdits モード追加
- v1.1.1: settings.json 権限精査 + プロファイル構文修正
- v1.1.0: Cross-Cutting Revision 実装
- v1.0.4: E2E Inspector 分離 + Lead Server Management
- v1.0.3: background-only SubAgent dispatch
- v1.0.2: default background dispatch
- v1.0.1: Builder Self-Check + Steering-Aware TaskGenerator

## Resume Instructions
1. `session.md` を読んで方向性を把握
2. 実プロジェクトで `install.sh --update` を実行して v1.2.0 に更新、`.claude/sdd/` → `.sdd/` マイグレーション動作を確認
3. acceptEdits モードで `.sdd/handover/decisions.md` 等の編集が確認プロンプトなしで通るか検証
4. 検証後、改善アイデア (A1/A2/A3/A5) の優先度を決定
