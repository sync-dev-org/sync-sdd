# Session Handover

## Direction

### Immediate Next Action
v1.2.1 リリース完了。実プロジェクトで `install.sh --update` を実行し、v1.2.1 の動作を検証する。特に compact 後の pipeline 続行動作を確認。

### Active Goals
- **実運用検証**: .sdd/ パス移行、compact 後 pipeline 続行、Builder git 制約、acceptEdits モードを実プロジェクトで検証
- 残りの改善アイデア: A1 (Context Budget), A2 (Run Fingerprint), A3 (Smart Retry), A5 (Adaptive Review Depth) -- 実運用検証の後に着手

### Key Decisions
- **開発方針**: 本リポはsync-sddフレームワーク自体の開発リポ。spec/steering/roadmapは不使用 (D2)
- **フェーズモデル維持**: 4フェーズのまま (D5)
- **Lookahead追跡は動的再計算** (D6)
- **sdd-review-selfはCommands(6)に含めない**: framework-internal用途 (D2)
- **SubAgent dispatch はデフォルト background**: `run_in_background: true` + agent frontmatter `background: true` (D10)
- **Worktree分離は不採用**: 現状で問題なし (D14)
- **Cross-Cutting は revise 拡張**: feature 名有無で自動判定 (D17)
- **.sdd/ はユーザー成果物**: uninstall でフレームワークファイルのみ削除 (D27)
- **compact 後 pipeline 続行**: roadmap run/revise 中は compact 後も pipeline を続行。ただし spec.yaml を ground truth として扱い、手動回復パッチは禁止 (D29 session)

### Warnings
- install先の `.claude/CLAUDE.md` と `.sdd/` は旧バージョンのまま。実プロジェクトで `install.sh --update` 実行が必要
- 競合リサーチのプランファイル (`~/.claude/plans/peaceful-noodling-comet.md`) に未実装アイデア 15件分の詳細あり

## Session Context

### Tone and Nuance
なし

### Steering Exceptions
なし

## Accomplished

### Work Summary (this session)
1. **compact 後 Spec 破損の根本原因分析**: 実践環境で cross-cutting revise 中に compact → Lead が spec.yaml を自己判断で「回復」修正 → Spec 破損。原因は (1) Builder の git stash がワークスペース全体に影響、(2) Session Resume Step 7 と Behavioral Rules の矛盾、(3) mid-pipeline state が conversation context にしか存在しない gap
2. **3つの修正適用**:
   - `CLAUDE.md` Session Resume Step 7: pipeline 続行時は spec.yaml を ground truth として扱い、不整合検出時はユーザーに報告（手動回復禁止）
   - `CLAUDE.md` Behavioral Rules: compact 後 pipeline 中は続行OK、非 pipeline は従来通りユーザー指示待ち
   - `sdd-builder.md`: workspace-wide git 操作 (git stash, checkout ., restore ., reset, clean) を禁止
3. **Self-review B7**: 5/5 エージェント完了。C:0 H:0 M:0 L:2。12件 FP 除去
4. **L1/L2 修正**: design-discovery-full.md 表記統一 + SKILL.md argument-hint dead-code feature 曖昧性解消
5. **v1.2.1 リリース**: branch + tag + push 完了

### Modified Files
- `framework/claude/CLAUDE.md` -- Session Resume + Behavioral Rules 改訂
- `framework/claude/agents/sdd-builder.md` -- git 操作禁止ルール追加
- `framework/claude/sdd/settings/rules/design-discovery-full.md` -- 表記統一
- `framework/claude/skills/sdd-roadmap/SKILL.md` -- argument-hint 修正
- `VERSION` -- 1.2.1
- `README.md` -- バージョン更新
- `install.sh` -- バージョン更新

### Previous Sessions (carry forward)
- v1.2.0: SDD data root 移行 (.claude/sdd/ -> .sdd/)
- v1.1.2: acceptEdits モード追加
- v1.1.1: settings.json 権限精査 + プロファイル構文修正
- v1.1.0: Cross-Cutting Revision 実装
- v1.0.4: E2E Inspector 分離 + Lead Server Management
- v1.0.3: background-only SubAgent dispatch
- v1.0.2: default background dispatch
- v1.0.1: Builder Self-Check + Steering-Aware TaskGenerator

## Resume Instructions
1. `session.md` を読んで方向性を把握
2. 実プロジェクトで `install.sh --update` を実行して v1.2.1 に更新
3. compact 後の pipeline 続行動作と Builder の git stash 禁止が機能しているか確認
4. 検証後、改善アイデア (A1/A2/A3/A5) の優先度を決定
