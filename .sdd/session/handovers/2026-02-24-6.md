# Session Handover

## Direction

### Immediate Next Action
v1.1.1 リリース完了。次の開発テーマを選択する（実運用検証 or 残りの改善アイデア）。

### Active Goals
- **実運用検証**: v1.0.4 の新機能 (E2E/Visual 分離、Lead Server Management) + v1.1.0 の Cross-Cutting revision + v1.1.1 の権限設定を実プロジェクトで検証
- 残りの改善アイデア: A1 (Context Budget), A2 (Run Fingerprint), A3 (Smart Retry), A5 (Adaptive Review Depth) など

### Key Decisions
- **開発方針**: 本リポはsync-sddフレームワーク自体の開発リポ。spec/steering/roadmapは不使用 (D2)
- **フェーズモデル維持**: 4フェーズのまま (D5)
- **Lookahead追跡は動的再計算** (D6)
- **sdd-review-selfはCommands(6)に含めない**: framework-internal用途 (D2)
- **SubAgent dispatch はデフォルト background**: `run_in_background: true` + agent frontmatter `background: true` (D10)
- **Worktree分離は不採用**: 現状で問題なし (D14)
- **E2E Inspector 分離**: 機能テスト vs デザイン品質で分離。Lead Server Management で並列実行
- **Cross-Cutting は revise 拡張**: 新サブコマンドではなく、`revise` の 2 モード統合。feature 名有無で自動判定 (D17)

### Warnings
- install先の `.claude/CLAUDE.md` は旧バージョンのまま。実プロジェクトで `install.sh --update` 実行が必要
- 競合リサーチのプランファイル (`~/.claude/plans/peaceful-noodling-comet.md`) に未実装アイデア 15件分の詳細あり

## Session Context

### Tone and Nuance
なし

### Steering Exceptions
なし

## Accomplished

### Work Summary (this session)
1. **settings.json 権限精査 (v1.1.1)**:
   - デッドエントリ 2件削除: `Bash(cat *)`, `Bash(echo *)` (CLAUDE.md 規約に矛盾)
   - Skill 許可 7件追加: 全 user-invocable skill の自動実行
   - Task 許可 24件追加: 全 SubAgent の background dispatch スタック防止
   - Bash 許可 11件追加: `git *`, `mkdir *`, `ls *`, `mv *`, `cp *`, `wc *`, `which *`, `diff *`, `playwright-cli *`, `npm *`, `npx *`
2. **プロファイル構文修正**:
   - 3ファイル (typescript/python/rust) のコロン区切り → スペース区切り (9箇所)
   - レガシー非推奨構文を正式構文に統一
3. **v1.1.1 リリース**: branch + tag + push 完了

### Modified Files
- `framework/claude/settings.json` — 全面書き換え (2→42 エントリ)
- `framework/claude/sdd/settings/profiles/typescript.md` — 構文修正 (5箇所)
- `framework/claude/sdd/settings/profiles/python.md` — 構文修正 (1箇所)
- `framework/claude/sdd/settings/profiles/rust.md` — 構文修正 (3箇所)
- `VERSION`, `README.md`, `install.sh` — v1.1.1

### Previous Sessions (carry forward)
- v1.1.0: Cross-Cutting Revision 実装
- v1.0.4: E2E Inspector 分離 + Lead Server Management
- v1.0.3: background-only SubAgent dispatch
- v1.0.2: default background dispatch
- v1.0.1: Builder Self-Check + Steering-Aware TaskGenerator

## Resume Instructions
1. `session.md` を読んで方向性を把握
2. 次の開発テーマを選択: 実運用検証 or 残りの改善アイデア (A1/A2/A3/A5)
3. 競合リサーチ (`~/.claude/plans/peaceful-noodling-comet.md`) に未実装アイデア多数あり、参照可能
