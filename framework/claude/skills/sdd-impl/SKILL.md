---
description: "[Redirect] Use /sdd-roadmap impl instead"
allowed-tools: Read
argument-hint: <feature-name> [task-numbers]
---

# SDD Implementation (Redirect)

<instructions>

This command has been unified into `/sdd-roadmap`.

Inform the user: "Implementation operations now use `/sdd-roadmap impl`. Redirecting..."

Then invoke `/sdd-roadmap impl {$ARGUMENTS}` via the Skill tool.

</instructions>
