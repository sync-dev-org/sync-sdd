# Knowledge entries accumulated during project lifecycle
# Write: Builder (impl/fix), Verdict, Dead-code, Reboot, Lead
# Read: Lead, Builder, Inspector, Architect, ConventionsScanner
entries: []
# Entry schema:
# - id: "K{N}"
#   status: "active|superseded"
#   severity: "H|M|L"
#   summary: "{one-line}"
#   detail: "{詳細・影響・推奨を含む}"
#   source: "{出所}"
#   created_at: "{ISO-8601}"
